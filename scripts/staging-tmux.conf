# Installed to ~/.tmux.conf by setup-staging.sh
set -g mouse on
set -g history-limit 50000
set -g default-terminal "tmux-256color"
set -g status-style "bg=colour236,fg=colour250"
set -g status-left "#[bold] #S "
set -g status-right " %Y-%m-%d %H:%M "
